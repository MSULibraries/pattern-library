<!-- Views: Format grid settings -->

<!-- Views: Custom row class -->
<div class="row pr-4">

    <!-- Views: Custom column class -->
    <div class="col-12 col-md-6 col-lg-4 col-xl-3 pr-0 pb-4">

        <!-- Views: Field - Custom text field -->
        <a href="{{ view_node }}" class="text-light">
            <div class="card card-hover-jump card-hover-info text-light bg-dark">
                <div class="card-fade">
                    <img src="{{ field_hero_image }}" class="card-img" alt="this is the alt text">
                </div>
                <div class="card-img-overlay p-4">
                    <div class="card-text">
                        <h4 class="h5 mb-2 text-shadow">{{ title }}</h4>
                        <div class="small text-shadow">{{ field_subtitle }}</div>
                    </div>
                </div>
            </div>
        </a>

    </div>
    
</div>
